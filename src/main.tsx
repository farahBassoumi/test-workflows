import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App' // Remove the file extension
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
        <h1 className='text-white'>hjk</h1>

    <App />
    <h1 className='text-white'>hjk</h1>
  </React.StrictMode>,

)
